# Core (see pyproject.toml for install: pip install -e ".[extras]")
torch>=2.0
numpy>=1.24
PyYAML>=6.0
tqdm>=4.66
matplotlib>=3.7
pytest>=7.4

# Optional (Stage 2+ tooling); install with pip install wandb einops datasets
# wandb
# einops
# datasets
